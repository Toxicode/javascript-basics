// Créer un oracle qui répond Oui ou Non en fonction de la taille de la question

/*
function startQuestionsGame() {
    var question = prompt('Posez votre question ?');
    var reponse;
    var suite = true;
    
    if (question === 'stop') {
        suite = false;
        reponse = 'merci d\'avoir joué'
    } else if (question.length >= 15) {
        reponse = 'Oui';
    } else {
        reponse = 'Non';
    }

    toxicode.console.log(reponse);
    if (suite) {
        startQuestionsGame();
    }
}

startQuestionsGame();
*/